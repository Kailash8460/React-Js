import React from 'react'

const User = (props) => {
    return (
        <div>
            <h2 style={{ textAlign: "center", color: "white" }}>{props.name}</h2>
        </div>
    )
}

export default User
